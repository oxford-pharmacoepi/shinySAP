# Add a codelist to a SAP

Add a codelist to a SAP

## Usage

``` r
addCodelist(sap, codelist)
```

## Arguments

- sap:

  A `sap` object.

- codelist:

  A `sap_codelist` object.

## Value

The updated `sap` object.
