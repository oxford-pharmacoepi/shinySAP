# Add a data-source modification to a SAP

Add a data-source modification to a SAP

## Usage

``` r
addDataSourceModification(sap, dataSourceModification)
```

## Arguments

- sap:

  A `sap` object.

- dataSourceModification:

  A `sap_data_source_modification` object.

## Value

The updated `sap` object.
