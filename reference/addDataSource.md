# Add a data source to a SAP

Add a data source to a SAP

## Usage

``` r
addDataSource(sap, dataSource)
```

## Arguments

- sap:

  A `sap` object.

- dataSource:

  A `sap_data_source` object.

## Value

The updated `sap` object.
