# Add a component to a SAP

Add a component to a SAP

## Usage

``` r
addSapComponent(sap, component)
```

## Arguments

- sap:

  A `sap` object.

- component:

  A `sap_*` component object.

## Value

The updated `sap` object.
