# Add a cohort to a SAP

Add a cohort to a SAP

## Usage

``` r
addCohort(sap, cohort)
```

## Arguments

- sap:

  A `sap` object.

- cohort:

  A `sap_cohort` object.

## Value

The updated `sap` object.
